packages:
  - 'packages/*'
  - 'apps/*'
allowBuilds:
  esbuild: true
# Supply-chain policy: yeni yayınlanan paketlerin (@babel/parser vb.)
# minimumReleaseAge kontrolüne takılmaması için gevşetildi.
minimumReleaseAge: 0
